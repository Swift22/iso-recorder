# cmd.exe needs CRLF; a .cmd with LF line endings breaks labels and goto.
*.cmd text eol=crlf
*.ps1 text eol=crlf
# The package README is a Windows text file, shipped inside the installer.
packaging/windows/README.txt text eol=crlf
